01-01-申请阿里云账号

要使用阿里云的服务，我们需要先去申请一个阿里云的帐号。打开 aliyun.com  ... 点击页面右上角的注册 ...    输入你的电子邮箱的地址  ...   
再设置一下密码 ...   再输入一次...  然后再输入你的手机号  ...  点击 获取免费短信验证码 ... 这样阿里云会往你的手机里发一条短信，在短信里面，会包含一个验证码 ... 
得到这个验证码以后，输入到下面的这个 短信验证码 这里  ...  
再输入一个图片的验证码 ...   然后点击 同意协议并注册 ... 
这样阿里云会往我们在注册的时候填写的邮箱里发一封验证的邮件  ...  下面，我们可以打开邮箱，找到这封邮件 ... 
先登录到邮件  ...    在收件箱里，找到阿里云发过来的验证邮件  ...   
然后点击 完成注册  ....   这样就可以成功注册一个阿里云账号了 ...


01-02-购买阿里云的 ECS 服务

下面，我们就去购买一个阿里云的 ECS 服务。先用之前注册的帐号登录一下  ... 
打开 aliyun.com  ... 然后点击 登录  ...   
输入我们在阿里云注册的用户名 ...   也就是当前注册的时候，填写的那个邮箱的地址  ...   再输入密码 ... 点击 登录  ... 
登录以后，打开 产品 菜单  ...  然后选择 云服务器 ECS  ...
再点一下页面右边的这个 立即购买 的按钮 ... 
这样会打开一个选择服务器配置的界面  ...   在这里，你可以选择需要的服务器的配置...  不同的配置，价格是不一样的  ... 
默认是  1 核的 CPU ...  512MB 的内存， 服务器的所在地是 青岛 ...  价格现在是 49.5  一个月，这个价格，在以后应该会有变动  ...
这里，我们可以选择大一点的内存 ... 选择 1G  ...
带宽可以是默认的 1M ，不够用，以后我们可以再升级 ...
再选择服务器的地域  ...    
这里，我们可以选择默认的青岛 ... 因为这个地方离我比较近 ... 而且也便宜一点   ...    注意选择的地域会影响到你以后使用的阿里云的其它的服务。比如关系型数据库，云存储服务等等，这些服务你可能都需要选择与 ECS 服务器相同的地域，这样会节省一点成本，而且也会更快一些 ... 
在镜像类型这里，可以选择 公共操作系统 ，或者 镜像市场 ...
公共操作系统就是，你可以在下面，去给服务器选择操作系统，然后你需要自己去配置服务器，比如安装 Web 的运行环境 ... 
另外，你也可以选择 镜像市场 ...   我们可以先看一下 ...  选择镜像市场 ..
然后点击 从镜像平台选择 ...
在这里，你可以选择配置好的镜像 ...  比如，你需要一个 PHP 的运行环境，可以选择自己想要的 php 的版本，web 服务器，还有操作系统。
选择这种镜像，在完成购买服务器以后，你就可以拥有一个立即可以使用的环境或者是其它的软件了。
不过，我推荐大家在使用这些镜像之前，可以先去自己试着配置一个 Web 的运行环境，这样你会更了解你的云服务器。
点击 关闭 ..
这里我们选择 公共操作系统  ...   然后再选择一下操作系统的类型 ...  选择 CentOS ...   它是一款 Linux 系统的发行版本 ... 
再选择一下操作系统的版本 ...   这里选择 6.5 ，64 位的 CentOS ... 
默认阿里云的云服务器，会自带一个 20G 的系统盘 ...  如果选择 Windows 系统的话，这个系统盘会是 40G  ... 
我们可以给云服务器添加一块数据盘 ... 
点击 添加 ...   再选择一下数据盘的大小 ...   我们先选择 20G，不够用的话以后可以再去升级  ... 
最后，再选择一下购买的时长 ...    这里我们先买一个月的时间 ....   再确定一下要购买的数量  ...  
然后点击 立即购买 ... 
确定以后，点击 去支付  ... 
选择支付的方式  ....  这里我选择的是使用支付宝去支付 ...  点击 确认支付 ...
再输入支付宝的帐号 ...  还有支付的密码 ...
点击 下一步  ...
确认付款 ...   
成功以后，我们就有了一台阿里云的 ECS 服务器 ... 
    进入管理控制台 ...
在这个控制台上， 会显示，我在青岛这个节点上，有一台阿里云的服务器 ...


01-03-云服务器的管理后台预览

先登录到阿里云 ... 然后打开 管理控制台 ...   选择 云服务器 ECS  ...  再找到我们在青岛节点购买的服务器 ... 
在这里， 你可以看到服务器的名称，公网还有内网的 IP 地址，服务器的状态等等 ...
这个公网的 IP 地址，就是一个对外的 IP 地址，也就是你需要把它跟你的网站的域名绑定到一块儿 ... 访问你的域名的时候，打开的就是这个服务器的公网的 IP 地址。
服务器的内网的 IP 地址，可以用来与内网之间的服务器进行通信 ...   比如，你想要做负载均衡之类的事情，可能就会用到这个内网的 IP 地址  ...  
再看一下，操作这栏内容 ... 
我们可以打开服务器的管理界面  ... 可以升级，续费 ...  
点击 更多操作  ...  在这里，还可以 停止 ，重启服务器，可以修改服务器的密码  ... 
我们先打开 管理 ...
在这个服务器的管理界面上，会显示服务器的更详细的配置 ...  
在下面，还会显示出服务器最近的 CPU，网络，还有磁盘的使用情况 ..
下面，我们可以去修改一下服务器名称 ...  让它更有意义一些 ...    点击 修改信息 ...


02-01-远程控制 Linux 系统的服务器

在购买了阿里云的服务器以后，会自动把服务器的相关信息发到我们在注册账户的时候，填写的手机上 ...  比如服务器 IP 地址，管理员的用户名，还有初始的一个密码。使用这些东西，我们就可以去管理和控制服务器了。
远程控制 Linux 类型的系统的服务器，一般不像 Windows 服务器那样，可以使用一个图形界面去远程控制。
我们需要使用命令行工具，远程连接到服务器，然后使用命令去控制服务器，比如去安装软件，启动服务等等。
Windows 用户可以使用 Putty 这个工具 ，Mac 用户可以使用系统自带的终端工具。
Web 终端
另外，阿里云也给我们提供了一个 Web 界面的终端工具 ...   也就是你可以直接使用浏览器去远程控制你的服务器。我们先去试一下它 ...
在服务器的管理界面上，找到 连接管理终端 ...  
第一次访问这个工具的时候，会给你一个初始的密码，使用这个密码，可以登录到这个工具上 ...   
这里，我自己设置了一个密码 ...  先输入密码 ...   然后点击 确定 ...
在这个工具的左上角这里，会显示连接的状态 ...  这里提示说，已经成功的连接到了我们这台服务器 ... 
如果发现一直是黑屏 ...   可以随便按一个键 ，去激活它 ... 
提示登录 ...  
在 login 的后面，输入用什么身份登录到服务器，也就是用服务器里面的哪一个用户登录 ...    在 linux 服务器里面，root 这个用户是最厉害的用户，它是系统的超级管理员，可以在服务器上做任何的事情 ... 
因为我们还没有为服务器创建其它的用户，所以，可以先用这个 root 用户登录到我们的服务器 ... 
输入 root  ...  表示要使用 root 这个用户登录 ... 
回车 ...  又提示 Password ，也就是让我们输入 root 这个用户的密码 ...   这个密码在我们购买云服务器成功以后，用短信的形式发到了我们填写的手机上。
如果忘了这个密码，可以在服务器管理界面重新修改一下 ....   修改好以后，重新启动服务器才能够生效 ... 
输入密码 ....   
然后回车 ...  
登录成功以后，你会看到一条欢迎的信息 ...  Welcome to aliyun Elastic Compute Service... 
下面的 root，表示当前登录的用户是 root 这个用户，在 @ 后面的这串字符，是你的服务器的最开始的那个名字 ... 
现在，我们就可以使用命令去控制服务器了 ... 
先输入一个 pwd  ...  
这行命令会显示出我们当前的位置  ...   返回的是 /root 　...  表示现在是在根目录下面的 root 这个目录里面 ... 
再用一个 ls  命令 ... 
它可以列出当前的目录里面的东西 ...


02-02-Windows：使用 putty 远程控制服务器

Windows 用户，可以使用 PuTTY 这个小工具，来远程的管理我们的 Linux 系统的阿里云服务器。
可以先去下载这个小工具  ...  打开 putty.org  ... 
在 Download PuTTY 这里，点击 here  ..
然后找到 PuTTY 的下载的链接   ...  点击这个 putty.exe ，可以把它下载下来  ... 
找到下载下来的 PuTTY  ....   打开它  ...   这个小工具不需要安装，可以直接使用  ... 
在 Host Name 这里，要输入我们的服务器的 公网的 IP 地址  ... 
在服务器的管理后台，可以找到这个地址  ... 
复制一下  ...
回过来  ...  然后再粘贴到这里 ... 
Port ，是端口号，可以使用默认的 22   ... 
下面的 Connection type ，连接的类型，选择默认的 SSH   ... 
然后点击 Open  ...  打开  ... 
提示 login as  ...   以什么身份登录到服务器  ...    我们可以使用 root 这个用户登录到服务器  ...  输入 root  ...
回车  ...  再输入这个用户的密码  ...   在完成购买服务器以后，这个 root 用户的初始的密码会发到我们的手机里  ... 
输入密码  ...  再按下回车 ...
出现这个欢迎的信息    ...   说明我们已经登录到了 Linux 系统的阿里云服务器上了  ... 
可以再试一下  ... 
输入 pwd  ...  这条命名会输出当前我们所在的位置  ...   返回的是 /root  ... 表示是在根目录下面的 root 这个目录里面  ... 
再输入 ls   ...  它可以列出当前目录里面的东西  ... 
要退出连接  ... 可以使用 exit 这个命令  ...


02-03-Mac：使用终端工具远程控制服务器

Mac 用户，可以使用系统自带的终端工具，去远程的控制我们的阿里云服务器。
使用快捷键 ctrl 加上空格 ... 用 spotlight 搜索一下  ...  输入一个字母 t  ...   选择 终端工具 ..
然后，我们可以使用 ssh 命令 ... 去登录到我们的服务器上  ... 
输入 ssh  空格  ...  再加上在服务器上的用户的名称  ... 也就是我们要用哪个用户登录到服务器 ...  这里使用 root 这个用户 ...  然后再输入一个 @ 符号 ...
后面加上服务器的公网的 IP 地址  ...   这个地址 ，可以在服务器的管理后台找到 ... 
在这里  ...  复制一下  ... 
再回到终端工具 ... 粘贴到这个 @ 符号的后面 ...    按下回车 ... 
提示确定想要继续连接吗？
输入 yes  ...  按下回车 ...
输入在服务器上的 root 这个用户的密码 ...    购买完服务器以后，这个密码会用短信的方式发送到我们的手机里面 ... 
输入密码 ... 
再按下回车  ... 
成功以后，会出现一条欢迎的信息 ...  欢迎来到阿里云的弹性计算服务  ... 
root 表示的是当前登录到服务器上的用户 ...   @ 后面这块是服务器的主机名 ... 
先输入一个 pwd  ...  可以输出我们当前所在的位置 ...
再输入一个 ls 　...  这条命令会列出当前这个目录里面的东西 ... 
使用快捷键 command + K ，可以清空一下终端上面的显示  ... 
输入 exit  ... 可以退出连接   ...


03-01-修改主机名

我们可以先用自己喜欢的工具登录到服务器。这里我用的是 Mac 自带的终端工具 ... 
登录成功以后，你会发现在这里会提示我们，当前登录的用记是 root 这个用户。
@ 符号后面的东西是服务器的主机名  ...  就是这个 AY 开头的字符串 ... 
下面我们可以使用 hostname ，去查看一下服务器当前的主机名  ...  输入 hostname  ...    回车  .. 
返回的跟 @ 符号后面的东西是一样的  ... 
下面，我们去修改一下它  ...  在 CentOS 系统里面，修改服务器的主机名，可以去修改一个配置文件 ...    这里我们使用 vim 编辑器去编辑一下这个配置文件 ...
先输入 vim  ... 后面加上这个配置文件的具体的位置 ... 是在根目录下面的 etc 下面的 sysconfig 这个目录里面的 network 这个文件 ... 
vim /etc/sysconfig/network 
回车 ... 
找到 HOSTNAME 这个选项 ..   在等号右边的东西就是服务器的主机名 ... 
先按一下键盘上的小 i 键 ...   进入到 vim 的编辑模式 ....    
我们可以修改一下 ...  改成 ninghao_server_1
完成以后，按一下 esc ，退出编辑模式 ...   再输入一个冒号  ...    wq ...   然后回车  ...   保存文件并退出 ...
再用 hostname 查看一下服务器的主机名 ..
下面我们需要重新启动一下服务器 ... 
回到服务器的管理后台 ... 
点击 重启  ...     这可能需要等几分钟  .... 
完成以后，再回到终端工具 ...  重新登录到服务器 ... 
现在，你会看到，在这个 root 后面的 @ 符号的右边  ...   就会变成我们修改之后的那个主机名了 ...


03-02-修改服务器的密码

阿里云会为你购买的 ECS 服务器设置一个初始的密码，并且会用短信的形式发送到你的手机上 ...  这个密码就是服务器 root 用户的密码。如果忘了这个密码，我们可以很容易在服务器的管理后台重新去设置一下。
登录到阿里云，打开你想要修改密码的服务器的管理后台 .... 
然后点击 修改密码 ... 
在弹出的对话框里，输入你想要设置的新的密码 ....  注意密码最小要包括 8 个字符 ... 最多是 30 个字符 ... 而且必须同时包含大写和小写的字母，还要有数字  .... 
再下面，重新再输入一次这个密码 .... 
然后点击 提交  ..
阿里云需要验证一下你的身份 ..  可以把验证码发到你在注册阿里云的时候填写的手机上 ... 或者，也可以发送到填写的邮箱里面  ...   这里我选择发送到我的手机里 .... 
点击 发送校验码 ... 
收到短信以后 ...   把验证码填写到下面的这个文本框里 .. .  
然后点击 确定  ...
再点一下 提交  ... .  确定要修改密码 ... 
这个新设置的密码，需要在服务器重新重动以后才能生效 ...   点击  重启  ... 
完成以后，我们就可以使用新的密码登录到服务器了。


03-03-格式化数据盘 - auto_fdisk.sh

我们为阿里云的 ECS 服务器购买的数据盘，需要先格式化一下，然后再挂载到系统里面才能使用  ...   在宁皓网以前出的 《在阿里云上运行网站》这个课程里面，详细的介绍过了格式化，还有挂载数据盘的方法。 你可以参考这个视频手工的去操作一下  ... 
现在，阿里云给我们提供了一个自动检测是否有需要格式化和挂载的小工具  ...    它可以很方便的去格式化和挂载数据盘  ...  
先用 root 用户，远程登录到服务器  ... 
在 root 用户的主目录下面的，可以找到这个小工具  ..    输入 ls   ... 查看一下  ...   这里的 auto_fdisk.sh 就是这个小工具  .. 
下面，我们先输入一个 df -h  ...  这行命名可以查看当前系统里面的磁盘的使用情况  ... 
你会发现，这里并没有我们为服务器购买的数据盘 。现在，只有一个系统盘  ..  大小是 20 G   ...  另外还有一个几百兆的临时文件系统  ...
下面，去执行一下自动格式化和挂载磁盘的小工具  ... 
先输入一个 bash  ..  后面加上这个小工具的名字  ...   auto_fdisk.sh   ...   回车  ... 
完成以后  ...   你会看到我们给服务器购买的数据盘  ... 
它被挂载到了根目录下面的 alidata1  这个目录的下面  .... 
再执行一下 df -h   ...   同样，你可以看到这个数据盘  ...   这个数据盘的大小，取决于你当时购买的数量   ... 
这样，想要把数据放到数据盘里的话  ...   就可以放在根目录下面的这个 alidata1 这个目录的下面。


04-01-Mac：无需密码登录服务器

每次远程登录 Linux 系统的服务器的时候，我们都需要指定一下用户名还有密码。下面，我们来看一下，使用 Mac 系统，不需要输入密码就能安全的远程登录到服务器的方法。
先打开终端工具 ...   我们需要使用命名生成一些密钥 ...  
输入 ssh-keygen -t rsa  ...  回车 ... 
提示正在生成 public 或 private rsa key pair  ...   这里会让我们指定一下这个生成的文件要保存到的位置 ...  默认就是在用户目录下面的 .ssh 这个目录里面 ... 名字是 id_rsa  ... 
按一下回车 ... 
提示输入密码短语 ...  不设置的话，可以直接按一下回车 ... 
然后再按一下回车 ... 
成功以后，提示我们的身份，已经保存到了当前登录到系统上的这个用户目录下面的 .ssh 这个目录里面了，文件的名字是 id_rsa ... 
另外，还生成了一个 public key ，一个公钥 ...  它保存在了当前登录到系统的用户目录下面的 .ssh 这个目录里面，文件名是 id_rsa.pub  ... 
我们需要复制一下这个文件里面的内存，然后到服务器上，去创建一个文件，这个文件里面的内容就是这个公钥文件里面的内容  ... 
先复制一下这个文件的路径  ... 
然后用 cat 这个命名 ...  后面加这个文件的具体的位置  ...  这样会直接给我们输出这个文件里面的内容  ...  复制一下  ... 
然后 command + K ，清空一下屏幕的显示  ...
再远程登录到服务器上  ...  用 ssh 命令 ... 使用 root 这个用户登录   ... 
输入密码 ... 回车 ...
登录成功以后，可以在当前这个用户的主目录下面，去新建一个叫 .ssh 的目录  ...  当前我用的是 root 这个用户登录的 ...  
用 mkdir 这个命名，可以去创建一个目录  ....  在这行命名的后面，加上要创建的目录的名称 ...   .ssh 
回车 ...
然后再用 vim ，去在这个目录下面创建并且编辑一个文件  ...  这个文件叫做  authorized_keys  ... 
按下小 i 进入到编辑模式  ...   把在我们本地电脑上生成的公钥粘贴到这里 ... 
再按一下 esc  ...   输入 :wq  ...  保存并退出 ...  
现在，我们就可以不需要密码登录到服务器了 ...   
先退出一下 ...   输入 exit  ... 
然后再试着登录到我们的服务器 ...     回车  ...    这次没有提示让我们输入密码 ... 直接就登录到了服务器  ... 
执行这行命名的时候  ...    服务器会用指定的这个用户目录下面的 authorized_keys 这个文件里面的内容，跟我们在本地电脑上的用户目录里面的 id_rsa.pub 去比较一下 ... 如果匹配的话，就让我们登录到服务器  ...


04-02-Windows：无需密码登录服务器 - 生成密钥

在这个视频里，我们一起看一下在 Windows 系统下面，使用 PuTTY 远程登录到服务器不需要输入密码的方法。
首先要去下载一下小工具  ...   打开 putty.org  ...   点击 here  ...   
在这个下载页面上 ... 找到 puTTYgen 这个小工具  ...   点击  puttygen.exe ，下载这个小工具  ...
下载完成以后，点击打开  ... 
如果你看到这个提示  ...  说明这个小程序被系统阻止了 ...  先点击确定  ... 
再去找到这个下载下来的小工具  ...   点击浏览器的设置按钮  ...  选择 下载内容 ...   在文件夹中显示 ..
找到以后，右键点击这个小程序  ...   选择 属性  ..
然后在这个常规选项卡里面，点击 解除锁定 ..    确定  ... 
再双击打开它  ... 
打开以后， 我们要用到它去生成一个密钥   ...   点击这个 Generate  ...   去生成一个密钥  ...
在生成这个密钥的时候  ...  鼠标要在这个空白的地方来回移动   ...   一直到生成完成为止  ... 
完成以后，在这个小窗口里显示的就是生成的密钥  ...   
先点击这个 Save private key  ... 保存私有密钥 ...
出现提示  ...   确定不设置密码保护吗 ?   点击  是 
然后选择一个地方，把它保存起来  ...   在我当前登录的这个用户的主目录下面  ...   新建一个目录  ...  命名为 ssh
输入要保存的这个文件的文件名 ...     ninghao_server_1_key
选中刚才创建的文件夹  ...   点击  打开  ...   然后再点击 保存  ...


04-03-Windows：无需密码登录服务器 - 把密钥保存在服务器里

下面，再打开 puTTY 这个小工具  ...   先登录到我们的服务器  ... 
输入服务器的 IP 地址  ...   点击 Open  ... 
用 root 这个用户登录   ...  输入 root 　...   再输入这个用户的密码  ...    然后回车  ... 
登录成功以后  ...    我们需要在当前登录的这个 root 用户的主目录下面，去创建一个叫 .ssh 的目录  ... 
输入 pwd  ... 查看一下当前所在的目录  ... 
然后再用 ls -la   ...  看一下这个目录里面的东西  ... 
在这里，已经有了一个叫 .ssh 的目录  ...   这是我在上一个视频里创建的  ...    如果没有这个目录，你需要使用 mkdir 这个命令，去创建一个  ...   
用 cd 命名，进入到这个目录里面 ...    清空一下屏幕的显示 .. 
再查看一下这个目录里面的东西  ...   
在这里，有一个叫 authorized_keys 的文件  ...  这也是我在上一个视频里创建的文件   ...     我们需要把之前用 puTTY Key Generator  生成的密钥，粘贴到这个文件里面。
可以先用 vim 打开这个文件  ... 
在这个文件里，已经有了一些内容，我们可以另起一行，去粘贴代码   ...  移动到这行代码的结尾处 ... 可以按一下 $ 键  ...   然后再按一下小 i  ... 进入到编辑模式 ...
另起一行  ... 
再去复制一下之前生成的密钥 ...     
再回到 puTTY
点一下鼠标的右键  ...  把复制的代码粘贴过来  ... 
按一下 esc  ... 退出编辑模式  ...   再输入 :wq   ... 保存并退出编辑的文件   ... 
再用一个 exit 命名，退出  ...


04-04-Windows：无需密码登录服务器 - 配置 puTTY

重新再打开 puTTY   ... 
我们可以去新建一个 session   ...   在这里输入一个名字  ...   ninghao_server_1  ...     点击 Save  ... 保存 ...      再选中这个新创建的 session  ... 
然后，在 Connection  这里，打开 Auth  ...   验证  .. 
Private key file for authentication  ...  点击它下面的 Browse  ..   浏览  ... 
找到之前我们保存成文件的那个密钥  ...        打开  ... 
下面，我们再去修改一下命令行工具的显示  ...   点击 Appearance   ...  外观  ... 
点击 Change  ... 修改一下字体  ... 
在这里，选择大一点的字号  ...     然后点击确定  ... 
现在，我们修改了 Auth ，验证  ...   还有 Appearance ，外观  ... 
再回到 Session   ... 
选中之前我们创建的这个 session   ...  ninghao_server_1  ...     然后点击  Save   ... 保存  ... 
把刚才的配置都保存在这个 session 里面  ... 
完成以后，点击 Open  ...  打开  ...   提示用哪个用户去登录到服务器  ...    输入 root  ...     然后回车  ... 
这次没有提示要输入 root 用户的密码，直接就登录到了服务器上  ... 
再试一下  .. .先退出  ...   
再打开 puTTY  ... 
然后双击 ninghao_server_1 这个 session   ...  
输入要登录的用户名  ...  root   ... 回车  ...   可以直接登录到服务器  ...


05-01-安装 Web 服务器 - Nginx

Nginx 是一款 Web 服务器，性能很好，占用的资源也小，所以很多流量比较大的网站都比较喜欢用 Nginx 作为 Web 服务器。
下面，我们就一起去给服务器安装一个 Nginx 服务器。
先用 root 用户，登录到服务器 ... 
然后可以再查看一下服务器的系统版本 ...  用 cat 命名  ...  输出在根目录下面的 etc 这个目录里面的 redhat-release 里面的东西 ... 
会显示当前服务器的系统版本是 CentOS 6.5  ... 
根据这个版本号，我们可以去安装对应的 Nginx  ...    打开这个地址  ... 
这里，有各种在 Linux 系统的安装 Nginx 的方法 ..    因为我们用的是 CentOS 6   ...  浏览到这个区域  ...    然后复制一个这个 CentOS 6 的链接的地址 ... 
回到终端工具 ... 
把复制的地址里面的东西下载下来  ... 可以使用 wget 命名  ...   后面加上这个链接的地址 ...     回车 ... 
下载完成以后  ... 可以用 ls 命名，查看一下当前目录里面的东西 ... 
这里这个 nginx-release 开头的文件，就是我们刚才下载下来的东西  ... 
再输入一个 rpm -ivh   ...    用 rpm 命名，去安装一个这个包 ...     小 i 表示安装  ...  v 表示显示详细信息  ...  h 表示显示安装的进度  ... 
复制一下这个文件的名称  ... 
粘贴过来  ...  然后回车 ..    安装完成以后  ... 可以再查看一下系统现在安装的资源包  ...
输入 yum repolist   ... 
这里的这个 nginx repo ，就是刚才安装的资源包  ...   现在，我们就可以使用 yum ，去安装 Nginx 了  ... 
输入 yum nginx install   ... 
确定要下载吗  ...  输入 y   ...   然后回车  ... 
这里会显示成功的安装了 nginx   ... 
清空一下屏幕的显示  ...   然后再用 service 命名，去查看一下 nginx 服务器的状态 ... 
service nginx status   ... 
提示 ...  nginx is stopped  ...  现在 nginx 服务器是停止的状态  ... 
下面我们可以去启动 nginx 服务器  ...    不过在启动之前，可以先测试一下 nginx 服务器的配置文件   ...  输入 nginx -t  ...
提示配置文件的语法正确  ...  测试通过  ... 
再用 service 去启动 nginx 服务  ...    service 后面加上要启动的服务的名字 ...  nginx  ... 再加上一个 start  ... 表示要启动服务  ... 
提示启动 nginx 服务成功 ... 
下面，我们就可以使用服务器的 ip 地址，或者是指向这个 ip 地址的域名访问到我们的服务器了  ... 
回到浏览器   ...  新建一个标签   ...  输入服务器的 ip 地址  ... 
如果能看到这个欢迎的页面 ...  说明在我们的服务器上，已经成功的安装了 nginx ，并且启动了 nginx 的服务  ....


05-02-设置域名指向服务器的 IP 地址

我们可以先去设置一下，让某个域名指向服务器的 IP 地址  ...    你可以打开所购买的域名的管理面板   ...  在不同的地方购买的域名，设置域名的方法和界面是不一样的  ...  不过你也可以使用第三方服务商提供的专门的 DNS 服务   ...  这里我用的就是 DNSPOD 来管理域名的 DNS，先登录到 DNSPOD  ... 
打开要设置的域名  ... 
点击 添加记录 ... 
先设置一个主机记录 ...  比如现在我们想要在访问 phoenix.ninghao.net 的时候，指向在阿里云购买的服务器的 IP 地址  ...   主机记录这里  ...  输入 phoenix ... 
记录的类型选择 A  ...   然后在记录值这里  ...   输入服务器的公网的 IP 地址  ... 
点击 保存  ... 
这样我们就设置好了一个地址，或者叫主机，也可以叫做二级域名   ...  就是这个  phoenix.ninghao.net  ...   在浏览器里访问它的时候，会把请求发送给我们的服务器。
点击 保存 ... 
测试
一般你需要等一段时间，这个设置才能生效 ...  不过 DNSPOD 提供的服务，基本上可以立即生效  ...   验证一下域名是不是已经指向了服务器的 IP 地址，Mac 用户可以使用终端工具，Windows 用户可以使用命令提示符工具  .. 
然后用 ping 命令 ...     后面加上设置的主机名  ...  phoenix.ninghao.net  ...   然后回车  ... 
ctrl + C ，可以停止使用这个  ping 命令  ...  
执行 ping 命令，会把后面的主机名解释成 IP 地址  ...  确定一下这里的 IP 地址是不是你的服务器的 IP 地址，如果不是，就说明，设置的这个主机名还没有生效  ... 
你可能需要再等一会儿  ...  然后再重新执行一下这条命令  ... 
ping 命令，会每隔一段时间会向服务器发送一个数据包，服务器如果返回了这个数据包，就说明你跟服务器之间的连接没有问题 ...  后面的 time ，是返回这个数据包用的时间 ...   这个时间越小，说明你跟服务器的之间的连接就越快  ... 
确定了设置好的主机名已经成效了 ...  我们就可以在浏览器里，直接使用这个主机名，来代替服务器的 IP  地址 ... 
回到浏览器  ...  新建一个标签  ... 输入之前设置的这个主机名 ...  phoenix.ninghao.net  ...  回车  ... 
你会看到，显示的跟之前用 ip 地址打开的是同样的内容  ...


05-03-配置 Nginx 的虚拟主机

你可以设置域名，让域名指向服务器的 IP 地址，在服务器上，你可以把指定的域名绑定到指定的某个目录上，这就是 Web 服务器的虚拟主机的功能。你可以让多个域名指向同一台服务器，绑定为不同的域名绑定不同的目录。这样访问这些域名的时候，就会打开我们服务器的指定目录里面的东西。
下面，我们一起来看一下 Nginx 的虚拟主机的功能。 
先登录到服务器  ...  输入 ssh root   ...  在 @ 符号的后面，因为之前我们已经让 phoenix.ninghao.net 这个域名指向了服务器的公网的 ip 地址，所以，这里可以用它来代替 ip 地址  ... 
确定要继续连接吗？ 输入 yes ..  然后回车  .. 
登录成功以后，先进入到 nginx 的配置文件所在的目录  ...   更改工作目录，可以使用 cd 命令  ...   nginx 的配置文件默认是在根目录下面的 etc .... nginx ...  conf.d   ... 
进来以后  ...  查看一下这个目录里面的东西  ...  输入 ls   ... 
在这里，会有一个默认的配置文件  ... 就是这个 default.conf  ..   下面，我们去复制一份这个文件 ..   用 cp 命名 ...   先是要复制的文件的名字  ...   接着是复制之后的文件的名字 ... 
把它命名为 phoenix.ninghao.net.conf   ...  这样以后我就知道哪个配置文件属于哪个主机名  ... 
再查看一下  ...  ls
在这里，你会看到刚才我们基于 default.conf 复制的这个文件 ...   再用 vim ，去编辑一下它 ..
在这个配置文件里面  ...  有一个用 server 定义的配置区块  ...     在这个配置里面  ...  listen  ... 表示要监听的端口号  ...  80 是 HTTP 协议默认的一个端口号  ... 
下面的 server_name  ...  表示服务器的名字  ..    它后面的这个东西就是这个虚拟主机绑定到一块儿的那个域名  ...   
按一下 $   ...  跳到行尾 ...   再按一下小 i ，进入到编辑模式  ...   先删除掉这个 localhost  ... 把它换成之前我们设置的域名  ...   phoenix.ninghao.net ...  这个域名已经指向了这个服务器的 ip 地址  ... 
再往下看  ... 
root   ...  就是根的意思 ...     在 location / 这里面，有一个 root   ...  我们可以先删除掉这里的 root   ...   按一下 esc   ...  再按两个小写的 d  ... 可以删除掉当前行  ... 
然后，我们直接在 server 里面，去设置一个 root  ... 
按下小 i 进入编辑模式  ..  输入 root  ...  它的后面，就是 phoenix.ninghao.net 这个域名的根目录  ...  也就是，访问 phoenix.ninghao.net，打开的就是这个 root 后面设置的目录里面的东西  ... 
这里我们设置成根目录下面的 alidata1  ...  www  ..   phoenix.ninghao.net  ... 
再回到这个 location / 。它的意思就是，基于 server 里面的 root 设置的目录的根目录  ...  修改一下它里面的 index 对应的值  ... 在这里，加上一个 index.php  ...  这个 index 设置的东西就是默认打开的文件 ...   如果在这个目录里面有 index.php ，就默认打开它  ...  如果没有，就去试一下 index.html，如果还没有，再去打开 index.htm  ..   都没有，就返回一个 404 的错误  ... 
在以后，我们需要再回来继续修改这个配置文件  ...  先保存一下  ..   输入 :wq  ...  
下面再去创建这个虚拟主机需要的目录  ...  进入到 alidata1  ... 
在这个目录下面创建一个 www  的目录  ...   再进入到这个目录  ...   再这个 www 里面，再创建一个 phoenix.ninghao.net ...  这个目录就是 phoenix.ninghao.net 这个域名的根目录  ..
想让修改的 nginx 的配置生效的话，需要去重新加载一下，或者重新启动一下 nginx ...   可以先测试一下配置文件有没有错误  ...  输入 nginx -t 
这里出现了一个错误  ..  directive root is not terminated by ;    意思就是，配置文件里面的 root 这个指令的结尾没有分号  .. 
再去编辑一下这个配置文件  ... 
找到 root 这个指令  ...   在它的结尾的地方 ... 添加一个分号  ..    保存  ... 
再测试一下  ...  这次就没有问题了  ...   然后再去重新加载一下 nginx   ...    service nginx reload  ...   使用这样的形式可以重新加载指定的服务的配置  ..  用一个 service ，加上服务的名字  ... 后面再加上 reload  .. 
提示 OK   ...   这样配置就会生效了  ... 
进入到 phoenix.ninghao.net 这个域名的根目录  ... 
在这个目录里面，新建一个叫 index.html 的文件  ...   然后在这个文件里输入点内容  ..    保存一下  ... 
回到浏览器  ...      打开 phoenix.ninghao.net   ...  刷新一下 ...   你会看到刚才我们在 phoenix.ninghao.net 的根目录下面创建的文件里面的内容  ... 
再回到用 ip 地址打开的这个标签   ...   刷新一下 ...   显示的还是原来的内容  ...


05-04-添加资源库 - epel, ius

使用 yum 命令，可以很方便的去管理在系统里面安装的软件，不过如果想用它来安装更多的软件，需要去给系统安装几个第三方提供的资源库。之前我们已经在系统里安装了一个 nginx 官方提供的一个资源库，然后用 yum 去安装了 nginx 。下面，需要再去添加两个资源库 ... 
我们可以先用 yum 搜索一下要安装的东西  ...  比如看看资源库里有没有 php5.5 ...   输入 yum search php55  ... 
提示，No Matches found  ... 没找到匹配的东西 ...  这样就不能用 yum 去安装 php5.5 了。
下面去为系统安装两个新的资源库 ...  然后再回来搜索一下 php5.5  ...  先查看一下系统的版本 ...   输入 cat /etc/redhat-release ..
返回的是，CentOS 6.5  ...   表示我现在用的系统是 CentOS 6.5 这个版本的 ... 
然后打开浏览器 ...   再打开这个地址 .... 
找到 CentOS 6 这个区域 ...  在新的标签里打开这两个链接  ... 可以按住 command ... windows 用户应该按住 ctrl 键 ... 再点击这里的链接 ... 
这是两个我们要安装到系统上的资源库 ...    先复制一下其中的一个资源库下载的链接  ... 
回到终端工具 ...  
用 wget 命令...  后面加上刚才复制的下载的地址 ...  回车  ...   这样可以把地址里的文件下载下来 ... 
再回到浏览器 ... 打开另一个资源库下载的地址 ...  复制一下这个下载的链接 ... 再回到终端 ...
输入 wget  ... 加上刚才复制的地址 ...  再回车 ... 
清空一下屏幕的显示  ...  
ls  ...  查看一下这个目录里面的东西 ... 
这里的 ius ，还有 epel 开头的两个文件就是刚才我们下载的资源库 ...   下面，再用 rpm 命令，去安装一下它们  ... 
输入 rpm ，后面再加上几个选项 ...  ivh ...  再复制一下要安装的文件的名称 ...  粘贴过来  ... 回车  ... 
这里出现一个警告 ..  意思是， 要安装这个 ius 开头的资源库，得先去安装一下这个 epel  ...  这里的 epel 表示的就是 Extra Packages for Enterprise Linux  ...
再试一下 ... rpm -ivh  ...  复制一下这个 epel 开头的文件  ...  粘贴过来  ..   
完成以后，再去安装一个这个 ius  ... 
都完成以后，再查看一下系统的资源库  ...  yum repolist ...
这里的 epel  ...  还有 ius   ....  就是我们刚才安装的资源库 ... 
下面，再去搜索一下 php 5.5  ...  yum search php55 
这次，会在资源库里面，找到 php 5.5  ...  还有很多其它的相关的扩展  ...


05-05-安装 PHP 与 PHP-FPM

php 与 nginx 配合在一起工作用的是 FastCGI。另外我们还需要一个 FastCGI 的进程管理工具，它可以让 php 和 nginx 更好的在一起工作 ...  这个进程管理工具就是 php-fpm  ... 
下面，我们先去安装一下 php ，还有 php-fpm  ...
先使用 root 用户登录到服务器 ... 
登录以后，去搜索一下 php 5.5 版本的 php-fpm  ...   使用 yum search php55u-fpm  .. 
如果没找到结果的话，按照上一个视频里介绍的，去给服务器添加 epel 还有 ius 这两个资源库 ... 
这里找到了可以使用的 php-fpm  ... 
下面，再用 yum ，去安装一下它  ...    yum install php55u-fpm  ...   然后回车 ... 
yum 会自动解决软件之间的依赖问题 ...   因为我们要安装的 php-fpm 依赖 php 本身 ... 所以，这里还会提示我们需要安装 5.5 版本的 php  ... 
确定安装  ...  输入 y ，再按一下回车 ..     再输入一个 y  ....  然后回车  ... 
这里会提示说 ... 已经安装了 php 5.5 版本的 php-fpm，另外还安装了它依赖的 5.5 版本的 php  ... 
完成以后，我们得去启动 php-fpm  ...   清空一下屏幕的显示  ...  command + K
用 service  ... 查看一下 php-fpm 的状态   ...   service php-fpm status  ... 
提示，现在 php-fpm 是停止的状态  ...   再用 service 去启动它  ...   service php-fpm start ...   提示启动成功  ...


05-06-让 Nginx 与 PHP 配合在一起工作

下面，我们就可以去配置一下 nginx ，让它可以执行 php 的代码  ...   先去编辑一下 phoenix.ninghao.net 这个虚拟主机的配置文件 ....  因为我们想在这个虚拟主机里面，去运行 php 的应用  ... 
可以使用 vim   ...  加上配置文件的地址  ...  是在根目录下的 etc   ... nginx  ... conf.d  ...  这个配置文件叫做 phoenix.ninghao.net.conf  ... 
按下右边的花括号可以向后移动一段  ...   左边的花括号可以向前移动一段  ... 
在这里，找到这段配置  ...  location 波浪号 ...  反作线，点 php  ... 
按下小 i 进入到编辑模式  ...  然后去掉这行代码最前面的 # 号 ...  使用这个 # 号可以注释掉这行代码   ...  去掉它，就是让这行代码生效 ..
再去掉最后这个结束的花括号前面的 # 号， include  前面的 # 号  ...  还有 fastcgi_param，fastcgi_index，还有 fastcgi_pass 这几行代码前面的 # 号 ...  
在这里，用了一个 fastcgi_pass 指令，设置了一下 FastCGI 服务器的地址。 127.0.0.1 就是本机的意思，后面的 9000 就是端口号  .. 意思就是，把 php 脚本通过本机上的 9000 这个端口传递给 FastCGI 服务器。
下面的 fastcgi_index 设置的就是默认打开的一个文件 ...  这里设置的是 index.php 。
fastcgi_param 设置的是 fastcgi 的一些相关的参数，这些参数会传递给 fastcgi 服务器  ...  SCRIPT_FILENAME 表示脚本文件的名字  ...  我们需要修改一下它 ...  把这个 /scripts 去掉  ...  换成 $document_root  ..  意思就是请求的 php 脚本文件就是在当前请求的这个目录里面。
完成以后，保存一下这个文件   ...   按下 esc  .. 再输入 :wq ...
重新再加载一下 nginx  ...  service nginx reload  ... 
再进入到 phoenix.ninghao.net 这个虚拟主机的主目录  ...  是在根目录下面的 alidata1 ... www  ...  phoenix.ninghao.net 这个目录  ..
查看一下这个目录里面的东西  ...  ls   ... 
有一个之前创建的 index.html   ...  可以先删掉它  ...  用 rm  ...  rm -rf  index.html  ... 
再查看一下  ...  ls
然后在这个目录的下面，新建一个 php 文件   ...  vim index.php   ...   进入到编辑模式 ...  输入 <?php phpinfo(); ?>  ... 
这个 phpinfo 函数，可以输出 php 相关的一些信息  ...   
保存  ... 
再回到浏览器  ...  打开 phoenix.ninghao.net  ... 
如果你能看到类似的页面的话 ... 就说明，我们已经成功的安装了 php ，并且，可以让 php 和 nginx 配合到一块儿  ...  
现在，我们搭建的这个 Web 环境，就可以去运行 php 应用了。
不过这个文件应该命名成一个其它的名字比较合适  ...   回到终端工具  ...   使用 mv 命名，重命名一下 index.php   ... 
命名为 phpinfo.php  ...  
再回到浏览器  ...   在这个地址里面，加上 phpinfo.php   ...   回车  ...  你可以看到跟之前一样的页面  ...


05-07-安装 PHP 的扩展

在运行 PHP 应用的时候，可能需要我们去安装一些额外的 PHP 扩展，一般，应用程序都会提示你它需要的 PHP 扩展，如果在你的 Web 运行环境里面，没有这个 PHP 扩展，我们就需要去安装一下  ... 
因为我们之前已经安装了两个第三方的资源库，所以，你应该可以使用 yum 搜索到自己想要安装的 PHP 扩展 ... 
比如我们需要一个叫 pdo 的 PHP 扩展 ...   
先登录到服务器 ...  输入 yum search pdo   ...  这样会在资源库里搜索出包含 pdo 这个关键词的资源  ...  你要自己判断需要的是哪一个  ...   因为我们想要的是一个 PHP 的扩展，所以，这个资源应该是用 php 开头的 ... 
而且我们的环境里面，安装的是 php5.5 ，所以， 需要的资源应该是这个 php55u-pdo   ... 
找到以后  ...  可以再用 yum 去安装它  ...   输入 yum install php55u-pdo  ... 
安装完成以后，回到浏览器  ... .打开之前创建的 phpinfo ... 
然后在这个页面上搜索一下 pdo  ...  
在这个页面上，现在并没有显示出安装了 pdo 这个扩展  ...   这是因为，我们需要去重新启动或者重新加载一下 nginx ，还有 php-fpm 服务 ...  才能使用新安装的 php 扩展 ... 
回到终端  ...   输入 service nginx restart  ...  它可以重启 nginx 这个服务  ... 或者，也可以使用 reload   ...  重新加载一下  ...   
再去重新加载一下 php-fpm   ...  service php-fpm reload  ... 
再回到浏览器  ...   刷新  ... 
查看一下 pdo   ... 
在这里，你会看到，现在我们的环境里面，已经安装了 pdo 这个扩展  ... 
再回到终端  ...  我们可以再去安装一些常用的 php 的扩展 ...     
php55u-mysqlnd
php55u-pdo
php55u-gd
php55u-mcrypt
php55u-mbstring
php55u-xmlrpc
php55u-pecl-jsonc
php55u-xml 
回车  ..   输入 y 确认要安装这些扩展  ... 
完成以后  ...  再去重新加载一下 nginx 还有 php-fpm   ...  
再回到浏览器  ...    在这个显示 php 相关信息的页面上，你会找到刚才我们安装的扩展  ...


05-08-文件与目录的权限 - 修改 PHP-FPM 的用户

在服务器上运行的 Web 应用里面，有些目录或者文件需要可以写入的权限，这就需要我们去为这些目录或者文件去分配合适的权限，一般可以把它们的拥有者设置成 php-fpm 和 nginx 服务使用的用户就可以了。
默认 nginx 服务使用的用户叫做 nginx ，而 php-fpm 使用的用户默认是 apache。我们可以把这些服务使用的用户，改成一个统一的用户，比如可以把 php-fpm 服务使用的的用户修改成 nginx 。
下面，我们先查看一下 php-fpm 服务这个进程  ...  查看系统的进程，用的是 ps 这个命名   ...   这里可以再搜索一下 php 相关的进程 ...  ps aux|grep php
在返回来的信息里面，你会发现  ...  php-fpm 这个服务都是用 apache 这个用户运行的  ... 
再去看一下 nginx 这个服务的用户  ...  ps aux|grep nginx  ... 
nginx 服务用的是 nginx 这个用户运行的  ... 
下面，我们可以去把 php-fpm 服务使用的用户修改成 nginx 这个用户  ... 
用 vim ，去编辑一下 php-fpm 的配置文件   ...   在根目录下面的 etc ，php-fpm.d 里面  ...   就是 php-fpm 的配置文件   ...  先进入到这个目录 ... 
查看一下这里面的东西  ...   ls ... 
这里有一个 www.conf  ...    去编辑一下这个文件   ... 
然后输入一个 / ，后面再加上 user  ...   可以在这个文件里面搜索 user 这个关键词  ...   按下小写的 n ，可以查看下一处包含 user 的地方 ... 
浏览到这个 user = apache 这行代码   ...  它的功能就是指定了 php-fpm 服务所使用的用户  ...     这里我们把 apache 改成  nginx   ... 
下面的 group  ...  是一个群组  ...   也可以把它改成 nginx ...
保存一下这个文件   ...  输入 :wq  ....
现在，php-fpm 还有 nginx 这两个服务都会使用同一个用户运行了，也就是 nginx   ...    然后把想要拥有写入权限的目录还有文件的拥有者，设置成 nginx 这个用户就可以了  ... 
比如我们去修改一下 phoenix.ninghao.net 这个虚拟主机的主目录的拥有者  ...  先进入到根目录下面的 alidata1   ...  www   ...   查看一下它里面的东西 ...   输入 ls   ... 
你会看到这个 phoenix.ninghao.net 这个目录  .. 
如果想显示的更详细一些，可以这样  ...  输入 ls  -la   ...  这样会显示出这个目录的拥有者，还有群组  ...  
现在，它的拥有者是 root 这个用户，所属的群组也就是 root   ... 
下面，我们把它的拥有者修改成 nginx  ...  可以使用 chown 命令  ..  chown -R  这个大写的 R 选项，可以递归的去修改目录的拥有者  ..   在后面加上要修改成的用户的名字 ...   nginx   ...  最后再加上要修改的目录  ...  
完成以后，再查看一下  ...   
现在， 你会看到， phoenix.ninghao.net 这个目录的拥有者，就已经变成了 nginx 这个用户了  ... 
最后，别忘了再重新加载一下 php-fpm  ... 
再查看一下进程  ...  
现在 php-fpm ，就会用 nginx  这个用户去运行了 。关于 Linux 系统的目录和文件的权限问题 ...  你可以参考宁皓网的 《CentOS：在阿里云上运行网站》这个课程。


05-09-安装 MySQL

MySQL 是一般的 PHP 应用需要的数据库管理系统。下面一起去给我们的服务器安装一个 MySQL 。 这里可以先用 yum 去资源库里搜索一下，看一下都有哪些可以用的版本 ... 
yum search mysql  ...
在这里，你会看到有 mysql 5. 5 的版本，还有 mysql 5.6 的版本 ...   我们可以去安装一下这个 5.6 版本的 mysql   ... 
输入 yum install mysql56u-server   ...    回车 ...
这里出现了一个错误   ...  说 mysql56u-libs 与 mysql-libs-5.1.73 有冲突  ...   
我们可以先删除掉这个 mysql-libs-5.1   ...  先复制一下这个名字 ... 
再输入 yum remove  ...  去删除点东西  ... 后面加上刚才复制的名字  ...   yum remove mysql-libs
然后回车  ... 
确定要删除这些东西吗  ...  输入 y   ...   再按一下回车  ..
完成以后，再去安装一下 5.6 版本的 mysql   ... 
yum install mysql56u-server  ...
会提示一大堆它依赖的东西 ...   需要下载 22 M 的资源  ...  确定安装  ...  输入 y   ...  再按下回车  ..     等两三分钟就差不多可以安装好了 ...
安好以后 ...  可以去启动一下 mysql  ...   用 service  ...   后面加上服务的名字  ...  这里就是 mysqld  ...   再加上一个 start  ... 
查看一下 mysqld 这个服务的状态   ...  service mysqld status  ...  提示正在运行 ....
最后，我们可以再去执行一下小工具  ...  去做一些安全相关的配置  ...   输入  mysql_secure_installation  ... 
它会问我们一些问题 ...     Enter current password for root ，输入当前 root 用户的密码  ...  因为我们还没有为 mysql 的 root 用户设置密码 ... 所以，直接按一下回车  ... 
又提示需要， Set root password 吗？ 要为 root 用户设置密码吗？ 输入 y  ... 
在 New password 的后面，输入 mysql 的 root 这个用户的密码  ...    
回车  ... 重新再输入一次 ...  再按下回车 ..
又会问我们 Remove anonymous users 吗？ 想要删除掉匿名用户吗  ...  输入 y ，删除掉匿名用户  ...
Disallow root login remotely? 不允许 root 远程登录吗，输入 y 
Remove test database and access to it? ，要删除 test 这个测试的数据库吗？ 输入 y 
最后提示 Reload privilege tables now   ...  现在就要重新加载设置的权限吗？再输入 y ，然后回车，确认一下 ... 
下面，我们可以登录到 mysql   ...    输入 mysql -u 后面加上登录用的用户名  ...  root   ..   再加上一个 -p ，表示需要输入密码 ...
回车  ...   再输入 root 用户的密码，注意这个 root 用户是 mysql 这个系统里面的用户，并不是操作系统的 root   ... 
它的密码，就是刚才我们在执行 mysql_secure_installation 的时候设置的那个  ...    回车 ... 
登录成功以后，我们就可以去控制 mysql 这个数据库管理系统了 ...  比如可以去创建新的数据库，可以执行 SQL 语言操纵数据库等等  ... 
这里，我们输入一个 show databases   ...  结尾的地方加上一个 ; 号 ...    回车  ...   
这行命令可以输出数据库系统里面现有的所有的数据库  ...


